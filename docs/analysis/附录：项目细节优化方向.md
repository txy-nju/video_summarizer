# 附录：项目细节优化记录

本文件记录已识别但暂未实施的技术债与重构候选项，供稳定阶段升级参考。

---

## [已完成] chunk_synthesizer_node 职责收敛与语义纯化

**问题描述（历史）**  
历史版本中，`chunk_synthesizer_node` 曾承担并发计算与聚合透传的混合职责，节点边界不清晰。

**落地方案（已完成）**

| 节点名 | 当前职责 | 备注 |
|---|---|---|
| `chunk_synthesizer_worker_node` | 单 chunk 融合 worker，输出增量字段（`chunk_summary` + `latency_ms.synthesizer`） | 仅负责单分片计算 |
| `chunk_synthesizer_node` | 顺序重建与结果透传 | 不再执行并发计算 |
| `chunk_results` reducer | 深度合并并行分支结果 | 统一承担 merge 语义 |

**涉及改动范围**
- `core/workflow/video_summary/nodes/chunk_synthesizer.py`：worker 输出改为纯增量；聚合节点仅重排透传
- `core/workflow/video_summary/state.py`：`chunk_results` reducer 改为递归深合并
- `core/workflow/video_summary/nodes/map_dispatcher.py`：移除历史兼容 payload 字段
- 测试文件：同步更新 send_api-only 断言

**当前状态**  
已完成 send_api-only 架构收敛并通过全量回归（110 passed, 1 skipped）。
